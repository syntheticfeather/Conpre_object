# smart-customer-service/requirements.txt

# Core LangChain
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.36,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-mcp-adapters>=0.1.7,<0.2.0

# LLM 集成（根据实际使用选择）
# langchain-anthropic==0.3.0  # 配合您已有的 anthropic
langchain-openai>=0.2.0,<0.3.0   # 如果用 OpenAI

# Vector Store
chromadb==0.5.0
numpy==1.26.4

# Web Framework
fastapi==0.115.0
uvicorn==0.30.0
python-dotenv==1.0.0
sse-starlette==2.0.0

# HTTP & Tools
httpx==0.27.0
anthropic==0.30.0
tavily-python>=0.7.0
openai>=1.0.0,<2.0.0  # 用于嵌入API调用

# JWT Token 解析
PyJWT>=2.8.0

# MongoDB 客户端
pymongo>=4.6.0

# 加密工具
cryptography>=41.0.0

# HuggingFace 模型缓存配置（可选）
# TRANSFORMERS_CACHE=/app/cache/huggingface
