# 综三任务

## 后端

> 综合设计三的后端任务聚焦于"智能客服 + 贷款业务"的进一步闭环：补齐客服对话历史的查询与管理、为系统引入网关限流与熔断保护、打通 AI 客服到人工客服的转接链路、实现账单导出与电子合同等对客凭证能力，并继续扩展 Agentic Workflow 的提示词、工具权限与个性化能力。整体在现有 Spring Boot（Java）+ FastAPI 智能客服（Python）+ MySQL/Redis/RabbitMQ/MongoDB 技术栈上迭代。

### 一、完善客服对话历史查询

对话历史已落库 MongoDB，但仅有内部取数方法，未对外暴露查询能力。本任务补齐"会话列表 + 消息分页 + 管理员溯源"的完整查询闭环。

#### 学习目标

- 掌握 MongoDB 聚合管道与分页查询
- 理解索引设计与查询命中原理
- 熟悉 JWT 鉴权下的数据隔离与角色校验

#### 任务

1. Python 侧新增按用户聚合的会话列表查询与按会话分页的消息查询
2. Java 网关层透传封装，保持"Java 作为统一入口"的架构
3. 为高频查询字段建立索引避免全表扫描
4. 用户侧强制数据隔离，管理员侧支持跨用户溯源

#### 学习内容

- MongoDB `find` + `skip`/`limit`、聚合管道 `$match`/`$sort`/`$slice`/`$project`
- B+Tree 索引原理与复合索引设计
- RESTful 列表接口与分页参数规范
- JWT 鉴权下的数据隔离与角色校验
- 统一返回体复用规范

#### 交付物

- Python 历史查询接口模块
- Java 网关层透传 Controller + Service
- MongoDB 索引初始化脚本
- 用户/管理员两套查询接口

### 二、Nginx 网关 + 分布式限流 + 熔断

为系统引入入口层与接口层的流量保护：Nginx 做反向代理与基础限流，Redis 做分布式限流，对智能客服等下游调用做熔断降级，防止突发流量或下游故障拖垮整体服务。

#### 学习目标

- 理解负载均衡算法与 Nginx 反向代理配置
- 掌握限流算法原理与分布式限流实现
- 熟悉熔断器三态模型与降级策略

#### 任务

1. Nginx 配置 upstream 与入口限流，开启静态资源缓存
2. Java 层基于 Redis 实现分布式限流，封装注解 + AOP 切面
3. 对调 Python 智能客服的链路引入熔断降级，配置兜底话术
4. 压测验证限流与熔断在并发场景下的表现

#### 学习内容

- Nginx `upstream`/`location`/`proxy_pass` 与负载均衡算法（轮询/加权/IP Hash）
- 令牌桶、漏桶、滑动窗口三种限流算法的原理与对比
- Redis + Lua 原子脚本与 Redisson 分布式限流
- 熔断器 Closed/Open/Half-Open 三态、失败阈值、半开探测
- 降级（Fallback）与线程池/信号量隔离；Sentinel / Resilience4j 的使用

#### 交付物

- `nginx.conf` 网关配置
- `@RateLimit` 注解 + AOP 切面
- 熔断降级配置与兜底话术
- Jmeter 压测脚本与验证报告

### 三、人工客服（转人工）

当 AI 无法解决用户问题（投诉、反复无法解答、用户主动要求）时，将会话转接给人工客服，形成"AI 优先、人工兜底"的闭环。

#### 学习目标

- 理解会话状态机设计与状态流转约束
- 掌握转人工触发机制与排队分配策略
- 熟悉实时通信技术选型与会话归档

#### 任务

1. 意图侧新增转人工意图或 COMPLAINT 升级动作，Java 侧枚举同步扩展
2. 设计会话数据模型（状态、客服、排队时间、消息）
3. 实现客服工作台：拉取队列、接入会话、回复消息、结束会话
4. 双向消息实时推送（WebSocket 或复用 SSE）
5. 客服角色校验，区分用户/管理员/客服三类权限

#### 学习内容

- 会话状态机：AI 处理中 → 转人工排队 → 人工接入 → 结束
- 转人工触发：意图识别与连续未命中降级策略
- 排队与分配算法：轮询、最少会话数
- WebSocket 与 SSE 的对比与选型
- 会话归档、满意度评价、工单溯源

#### 交付物

- 转人工意图与枚举扩展
- 会话/消息数据模型
- 客服工作台接口与前端页面
- 双向实时推送通道
- 客服角色权限校验

### 四、账单导出 / 电子合同

为用户提供贷款账单导出与借款电子合同生成能力，作为对客可下载凭证，沉淀为可审计、防篡改的文件。

#### 学习目标

- 掌握 Excel/PDF 生成与流式导出
- 理解合同防篡改与异步任务编排
- 熟悉文件下载与权限校验

#### 任务

1. 账单导出：根据订单还款计划生成 Excel 文件流下载
2. 电子合同：放款成功后异步生成借款合同 PDF，落盘并回写路径
3. 大数据量导出走异步任务，完成后通过通知/SSE 链路告知用户
4. 下载接口带权限校验与文件名脱敏

#### 学习内容

- EasyExcel / Apache POI 生成 Excel，流式导出避免 OOM
- iText / OpenPDF、模板引擎（FreeMarker + HTML→PDF）生成合同
- 合同编号规则与内容哈希防篡改
- 异步任务编排：Outbox + RabbitMQ 或线程池
- 文件下载 Content-Disposition 与权限校验

#### 交付物

- 账单导出接口与 Excel 模板
- 电子合同 PDF 生成服务与模板
- 异步导出任务与完成通知
- 下载接口与权限校验

### 五、Workflow 扩展

在已完成的 Agentic Workflow（路由分类 + Java 固定流程 + Python 可组合 Agent 链）基础上，继续扩展：提示词精细化、工具权限表、新增工具、个性化产品推荐、引入 Skill 机制。

#### 学习目标

- 理解提示词分层与按意图动态绑定
- 掌握工具权限模型（RBAC）与工具注册机制
- 熟悉基于内容的推荐算法与冷启动
- 理解 Agent Skill 模块化封装思想

#### 任务

1. 提示词按意图/场景绑定不同版本，Agent 组装时动态选择激活
2. 新增工具权限集合，Agent 注入工具时按用户角色过滤
3. 扩展工具集：还款明细查询、信用分查询、产品推荐等
4. 结合长期记忆与产品特征，实现个性化产品推荐并接入 Agent 链
5. 将常见能力封装为 Skill（提示词 + 工具 + 权限），按场景动态加载

#### 学习内容

- 提示词工程：分层设计（系统规则/动态注入/业务规则/风格规则）
- 工具权限模型（RBAC）：角色 → 允许工具列表
- 工具注册机制：静态工具 + MCP 动态工具
- 基于内容的推荐：用户画像 + 产品特征匹配、规则打分、冷启动
- Agent Skill 概念：能力打包与按需加载

#### 交付物

- 按意图绑定的提示词版本管理
- 工具权限集合与角色过滤逻辑
- 新增工具集（还款明细、信用分、推荐等）
- 个性化推荐服务与 Agent 链接入
- Skill 封装与按场景动态加载机制

## 风控

### 风控多数据融合

在已有 XGBoost 风控模型基础上，将单一数据源扩展为多源数据融合：整合用户基础信息、业务行为数据、外部征信与设备指纹等，丰富特征维度，并通过规则引擎与模型双轨决策，提升风控精度与可解释性。

#### 学习目标

- 掌握特征工程与多源数据融合方法
- 理解规则引擎与模型双轨决策模式
- 熟悉模型可解释性、稳定性监控与漂移告警

#### 任务

1. 梳理多源数据：内部库表、外部征信、设备指纹、通讯行为等
2. 构建特征工厂，完成原始数据到模型可用特征的采集、编码与衍生
3. 规则引擎前置过滤（硬规则直接拒绝），模型打分决策（阈值分级通过/拒绝/转人工）
4. 对接推理链路：申请流自动调用风控打分，结果写入审核记录
5. 接入 SHAP 可解释性输出与 PSI 稳定性监控

#### 学习内容

- 特征工程：数值化、编码、分箱、WOE/IV 编码、衍生特征构造
- 多源数据融合：内部库 + 外部征信 + 设备指纹 + 通讯行为
- 规则引擎（Drools 或自实现）+ 模型双轨决策
- XGBoost 特征重要性与 SHAP 值解释
- PSI 稳定性指标与特征漂移监控
- 冷启动：新用户无历史数据时的兜底策略

#### 交付物

- 多源数据采集与特征工程服务
- 规则引擎配置与模型推理服务
- 申请流风控自动调用链路
- 拒绝原因 SHAP 解释输出
- 特征稳定性 PSI 监控与告警

## 前端

### 一、地基层：设计令牌与布局无缝衔接

统一全站设计语言，并修复「画布-侧栏-顶栏-主卡片」四层之间的衔接瑕疵与滚动割裂感，为后续所有视觉/交互改造奠基。

#### 学习目标

- 掌握 CSS 变量作为「设计令牌」的组织方式，建立品牌色、间距、排版、动效、圆角五套系统
- 掌握「共享画布 + 浮层卡片」的整站布局架构，理解透明层露出共享渐变、主卡片贴平顶栏的无缝衔接原理
- 理解内容经过容器边界时的「割裂感」来源，能用「渐隐带 + 内阴影 + 美化滚动条」组合手段消除边界感
- 熟悉 Element Plus 主题变量的一次性全局覆盖方案，避免在各组件中重复覆盖默认颜色

#### 任务

1. 在 `base.css` 中补全品牌色、间距、排版、动效、圆角五套设计令牌，并在亮/暗双主题下保持一致性；同时通过 Element Plus 全局变量一次性覆盖其默认主色与语义色，所有组件不再出现旧的 Element 默认配色
2. 修正 `index.html`：补全语言、标题、主题色，建立字体与关键资源的预连接，保证首屏 LCP 指标健康
3. 重构 `DashboardView.vue` 布局：紫→蓝→青共享渐变画布作为整站最外层背景；侧栏与顶栏改为透明，直接露出画布；主卡片改为「顶部贴平顶栏、左下/右下圆角、仅左右底三条半透明白边」的浮层形态，并移除顶部圆角导致的露紫露青问题
4. 解决滚动内容经过主卡片边界时的割裂感：主卡片顶部与底部各加一条渐隐带、底部增加「越往下越暗」的内阴影、滚动条瘦身并主题化，四层机制组合消除边界感

#### 学习内容

- CSS 自定义属性（设计令牌）的分层：语义令牌 → 组件令牌的两级关系
- `backdrop-filter`、`linear-gradient`、`inset box-shadow` 组合做浮层材质的原理
- `overflow`、`z-index`、`::before/::after` 伪元素作为装饰/渐隐层的惯用写法
- Vue 单文件组件中 `scoped` 样式的渗透写法（`:deep()`）、以及 Element Plus 主题变量覆盖的注入方式
- 亮暗双主题的切换机制与「同语义令牌不同取值」的组织方式

#### 交付物

- 「设计令牌 + 布局无缝衔接」补丁，涉及 `base.css`、`index.html`、`DashboardView.vue` 三个文件

---

### 二、顶栏交互：毛玻璃通知面板与管理员菜单

将顶栏的通知、管理员两个下拉弹窗从「便利贴式方块按钮 + 硬白底」升级为品牌画布呼应的毛玻璃材质与极简条目菜单，作为甲方登录后第一眼能感知到的「精致度」。

#### 学习目标

- 掌握毛玻璃（Glassmorphism）材质的核心三要素：半透明背景、`backdrop-filter` 高斯模糊、1px 半透明白边
- 理解「阴影颜色取自画布主色」的重要性（品牌色投影 vs 默认黑投影的质感差距）
- 掌握微位移型 hover 反馈的表达力（padding-left 增量代替粗重方块按钮）
- 掌握 Vue `<Transition>` 做「弹出/收起」的进场过渡结构

#### 任务

1. 通知面板材质化：背景改为 `backdrop-filter` 毛玻璃，阴影取品牌紫调深阴影，边框改为半透明白边；亮暗双主题分别适配
2. 未读通知的强调方式改造：左边框改为品牌青，并以品牌青浅渐变的左→右横向色带做背景，替换默认的天蓝硬边+浅蓝底
3. 通知面板头部「全部已读 / 清空全部」按钮简化为 link 风：无边框、hover 时轻微向左位移+品牌青变色，替换 Element 方蓝/红方块按钮
4. 管理员菜单「修改头像 / 退出登录」重做为极简条目菜单：图标+文字的横向条目，hover 时品牌蓝浅底+微左移，替换方块按钮
5. 通知面板与管理员菜单都包一层 Vue `<Transition>`，进场为淡入+轻微上移，离场反向

#### 学习内容

- `backdrop-filter` 的浏览器兼容、亮暗主题下的取值调整、`saturate()` 增加色浓度的用法
- B 端极简菜单交互：Linear/Notion 风格的「无边框 link + hover 位移」设计模式
- Vue `<Transition>` 组件的 `mode="out-in"`、`enter-active/leave-active` 钩子与动效令牌衔接方式
- 「阴影颜色取自主品牌色」的设计思维：在渐变画布上，黑色阴影会显得脏，品牌色投影则更统一

#### 交付物

- 顶栏弹窗质感升级补丁，主要涉及 `NavbarMenu.vue`

---

### 三、主列表页：空态装饰、多数据分组与快捷筛选

面向三大主列表（贷款申请 / 用户管理 / 贷款产品）以及知识库、MCP 工具，解决「数据少时空」「数据多时单调」「信息密度低但视觉刺激为 0」三个问题。

#### 学习目标

- 掌握「列表页的三态设计」：空态（0 条）、小数据集（1-5 条）、大数据集（≥8 条）各有不同的视觉策略
- 理解「装饰背景只作用于空区，不干扰数据可读性」的边界：点阵/网格图案用 data URI、低透明度、pointer-events:none
- 掌握「分组分割线」「hover 高亮左边框」「chip 筛选条」三种高 ROI 的列表设计增强手法，并能和筛选逻辑联动
- 学会判断何时用「表格」何时用「卡片流」：结构化数值数据用表格，阅读型文本内容用卡片

#### 任务

1. 三大主列表的「少数据态（≤3 条）」改造：在表格包裹层新增极淡的 SVG 点阵/网格装饰图案作为 data URI 背景（低透明度、不阻点击），并将表格最小高度拉高、行 padding 适当放宽；空态（0 条）用引导插画 + CTA 按钮替换「暂无数据」
2. 三大主列表的「多数据态（≥8 条）」改造：移除 Element 默认的 stripe zebra；每行 hover 改为「品牌蓝极淡底 + 品牌青 3px 左边框」并加过渡；每 5 行插入一条居中的软性分割线帮助用户按组扫视；所有数值/金额列启用 tabular-nums 做小数点对齐；表格外框统一圆角并 overflow:hidden 消除表头直角外露
3. 三大主列表在搜索条和表格之间新增 chip 快捷筛选条：全部 / 成功态 / 警告态 / 危险态 / 本周新增 / 高优先级 共 6 颗 round chip；点击 chip 直接激活对应筛选；激活 chip 切换为 primary+dark 形态
4. 知识库问答对 Tab：移除默认表格和 ID 列，改为「2 列卡片流（小屏降级 1 列）+ 视图切换按钮（卡片/表格）」；单卡含分类 tag、更新时间、问题、答案预览（3 行省略）、编辑/删除/展开全文操作；点击卡身可平滑展开答案全文
5. 知识库文档块 Tab：移除 ID 列；重新分配「文档名称 / 章节路径 / 内容预览 / 分块序号 / 操作」的列宽占比；内容预览加 3 行省略 + tooltip
6. MCP 工具管理 Tab：移除默认表格，改为卡片网格视图（≥中等屏 3 列、小屏 2 列）；每卡含分类图标、名称 1 行省略、描述 3 行省略、启用开关、来源 tag、查看详情 link-btn

#### 学习内容

- 装饰型 data URI SVG 图案（点阵/网格）的编写与注入背景
- `font-variant-numeric: tabular-nums` 等宽数字对齐的意义与用法（金额表对齐必备）
- 「表格 vs 卡片流」的信息架构取舍：字段数 × 行密度 × 字段语义（数值/文本）三因素决策
- 筛选系统分层：chip（低频常用标签）+ 表单（复杂搜索）+ 视图切换（表格/卡片）

#### 交付物

- 主列表整体优化补丁：
  - 三大主列表（贷款申请/用户管理/贷款产品）
  - 知识库问答对 Tab：表格→卡片流；文档块 Tab：列重分配
  - MCP 工具 Tab：表格→卡片网格

---

### 四、详情页：英雄头 + 粘性导航 + 分段式浏览

面向申请详情、用户详情、产品详情三个「阅读型页面」，用统一的骨架重做：粘性英雄头 + 紫青渐变 Hero 数据卡 + 分段导航 Tab + 滚动锚点联动。在保证可读性的前提下植入品牌营销高级感。

#### 学习目标

- 掌握「粘性英雄头（Sticky Header）+ backdrop-filter 模糊」的阅读页导航模式，理解「滚动时加阴影/不加阴影」两态切换的实现（IntersectionObserver 或 scroll 监听 + class 切换）
- 掌握「Hero 卡圆形数据徽章」设计：信用评分圆环、利率大字、金额大字三种数据可视化模式
- 掌握「自制分段导航 Tab」替代 Element 默认 tabs：背景容器化、激活态白底阴影，作为阅读页的主导航模式
- 掌握「滚动锚点 + Tab 自动高亮」的联动实现，理解 IntersectionObserver 在多段内容中的配置方式

#### 任务

1. 三大详情页统一骨架：
   - 顶部「粘性英雄头」：滚动时 position: sticky 置顶，`backdrop-filter` 半透模糊；下方 1px 品牌色浅边；滚动超过阈值后加阴影 class
   - Hero 数据总览卡：紫→青浅渐变背景做装饰底；左侧头像/标题/元信息；右侧突出圆形徽章（申请详情=贷款金额；用户详情=信用评分圆环进度；产品详情=平均年化利率）
   - 自研「分段导航 Tab」：容器浅灰底圆角，激活项白底 + 品牌字色 + 柔和阴影，非激活项透明过渡；不再用 Element 下划线 tabs
   - 滚动时 Tab 与内容锚点自动联动高亮（IntersectionObserver 实现）
2. 申请详情具体 Tab 拆分：「申请信息 / 用户信息 / AI 风控建议 / 审核流程时间线 / 操作区」；AI 风控建议 Tab 中含圆环评分 + 风险点 tag 列表 + 建议动作；审核流程采用竖向时间线节点
3. 用户详情具体 Tab 拆分：「基本信息 / 认证材料 / 借款记录 / 还款记录 / 风控标签」；认证材料 Tab 采用 6 格卡片布局（已上传缩略图+小绿标/未上传灰占位）；还款记录 Tab 采用月份网格还款状态或时间线；风控标签区支持 chip 增删
4. 产品详情具体 Tab 拆分：「可选方案 / 申请条件 / 还款说明 / 推广配置」；申请条件、还款说明、推广配置三 Tab 均采用「分段卡片 + 左侧 3px 装饰条」结构
5. 所有详情页的每段 section header 左侧统一 3px 紫→青渐变装饰条，段间距采用 XL 级统一

#### 学习内容

- IntersectionObserver 的 threshold、rootMargin 参数配置，用于「多段滚动时 Tab 自动高亮」
- SVG 圆环形进度图：`stroke-dasharray` / `stroke-dashoffset` 做动画的基础方法
- 自制分段 Tab 的两种实现：CSS class 切换 + transition；或 Vue `<transition-group>` 做下划线/高亮位移滑动（更高级感）
- 时间线组件的基础实现：竖向线 + 节点圆 + 左右分支布局

#### 交付物

- 三大详情页重做补丁：
  - 申请详情（ApplicationDetail 组件，先去掉模态壳改为 Panel 版，供第 5 批双栏使用）
  - 用户详情（UserDetailPanel 重写骨架）
  - 产品详情（ProductDetailPanel 重写骨架）

---

### 五、双栏联动：列表→详情丝滑切换（Master-Detail）

实现申请列表点击行后的「列表视觉左移收缩 + 详情面板从右侧推挤进入」的丝滑双栏联动，作为整个后台系统交互的最大亮点；并在小屏宽度下自动降级为详情全屏覆盖模式。

#### 学习目标

- 掌握 Master-Detail 双栏的「同容器宽度联动」过渡架构：同一 flex 容器内两个面板的 `flex-basis` + `transform` 联动过渡
- 理解「列表先动、详情跟进」的节奏设计（transition-delay），让推挤感更真实
- 理解「退到背景」的视觉提示：列表缩小至 42% 时配合 `transform: scale(0.985)` + `opacity` 轻微降低，让用户感知「我正在看的是另一层」
- 掌握响应式降级：屏幕宽度不够时双栏自动切换为全屏覆盖的移动友好模式

#### 任务

1. 在 `PendingApplicationView.vue` 与 `CompletedApplicationView.vue` 中，把原有「列表 + 模态弹窗」改为外层 `.split-container` 容器，内层为 `.list-pane` + `.detail-pane` 两个 flex 面板
2. 默认态：列表 100%，详情 `translateX(+120%)` 藏在右边界外、opacity=0
3. 激活态（`selectedRequestId != null`）：列表 flex-basis 缩至约 42% 并轻微 scale 降低透明度；详情 translateX(0) 淡入并占据剩余宽度；过渡时长统一取动效令牌的 normal；详情 pane 加少量 delay 让列表先动以制造推挤感
4. 激活行视觉锚点：选中行左边框 3px 品牌青 + 背景淡品牌青底，与高亮列表条目建立呼应
5. 关闭详情：移除选中 ID，反向过渡（详情先出、列表跟进）恢复列表 100%
6. 小屏降级：宽度低于阈值（约 1024px）时自动切换为「详情全屏覆盖」模式，不再执行双栏缩窄；以保证在小屏下可操作

#### 学习内容

- flex-basis 与 width 在动画中表现差异的理解
- `translateX` 位移 + opacity 做「从屏幕外进入」的标准写法
- 「先动/后动」的时间差安排：`transition-delay` 做节奏感
- 媒体查询与响应式降级：`@media (max-width: ...)` 触发布局模式切换

#### 交付物

- 双栏联动补丁：`PendingApplicationView.vue`、`CompletedApplicationView.vue`，以及由申请详情页改造而来的非模态 Panel 版

---

### 六、收尾：风控监控中心重做 / 系统页重做 / 路由过渡

对风控监控中心、Agent 配置、MCP 服务器管理、以及路由切换过渡做整体一致性收尾，保证全平台风格统一、交互流畅。

#### 学习目标

- 掌握「4 层信息架构」的数据 Dashboard 搭建：Page Title → KPI 区 → 近景区（动态 + 审批） → 远景区（趋势 + 分布图），各层之间的视觉锚点与间距分层
- 学会用「卡片化堆叠列表」替代表格展示 Agent prompt、MCP 服务器这类「半文本半配置」型条目，提高可读性
- 掌握 Vue `<Transition>` 包裹 `<router-view>` 的路由页面过渡，告别白屏硬切
- 理解「数字 CountUp + 图表入场动画」在首屏感知中的价值（不增加信息但增强「鲜活感」）

#### 任务

1. **风险监控中心（RiskManageView）**：按 4 层架构分区——(1) 标题条（风险监控中心 + 快捷按钮大屏入口）；(2) KPI 四张卡（每张加左侧渐变装饰条、趋势徽标用语义色胶囊背景、数据故事化：数值 + 趋势对比 + 上下文说明）；(3) 近景区（最新动态 + 月度审批，等宽双列）；(4) 远景区（申请趋势大图 + 右侧 2 小卡：风险分布环形图 + 用途分布进度条列表）；各层间距统一切到设计令牌
2. **Agent 配置（AgentConfigView）**：改为双栏布局——左栏新增/编辑 Prompt 表单，三个大 textarea（角色定义 / 业务规则 / 回复风格）改为代码编辑器感的灰底+等宽字体，标签与输入框融入；右栏为「提示词列表」，从表格改为卡片堆叠视图，每张卡含名称+版本徽标+激活开关+更新时间+操作按钮，激活/停用卡片在左侧加 3px 语义色边
3. **MCP 服务器 Tab（MCPToolsView）**：将「添加服务器表单」从首屏常驻大卡改为点击右上角 CTA 才展开的折叠面板（用 Vue `<Transition>` 包折叠动画），首屏优先展示已配置服务器列表；搜索+刷新移入 tab-toolbar 右对齐
4. **路由过渡与数字 CountUp**：在 `DashboardView.vue` 的 `<router-view>` 外包一层 Vue `<Transition name="page">`，进场为 opacity + 轻微 y 位移；所有 KPI 数值在首屏挂载时用 CountUp 做 0→目标值滚动（可用轻量库或自实现 rAF 动画），统一时长取 slow 令牌

#### 学习内容

- 「4 层信息架构」在数据密集后台页面的通用套用范式
- ECharts 入场动画的统一配置：`animationDuration`、`animationEasing` 取令牌值，避免各页乱配置
- `<router-view v-slot="{ Component }">` + `<Transition>` + `<component :is="Component">` 的 2024 年 Vue 3 标准写法
- 「折叠面板 + CTA 按钮」的首屏信息密度控制：默认展示用户最常用的（列表），次级动作（添加）按需展开

#### 交付物

- 风控监控中心 / Agent / MCP 重做 + 路由过渡 + 数字 CountUp 补丁：
  - 风控监控中心分层重写
  - Agent 配置：双栏 + 代码感编辑区 + 卡片堆叠列表
  - MCP 服务器：添加表单折叠化 + Tab 工具栏
  - Dashboard 路由过渡 + KPI 数字 CountUp

## 移动端

### 架构层

#### A1. Repository 统一先缓存后网络

**学习目标：** 理解 Repository 作为数据单一来源的职责，掌握 Room + Kotlin Flow 实现离线优先架构，建立"UI 永远先展示已有数据"的设计原则

**任务：** 重构 UserRepository、LoanOrderRepository、AuthRepository，统一数据获取策略——页面打开时先读 Room 缓存即时展示，后台静默请求网络，成功后更新 Room 并通知 UI 刷新，失败则不影响已有缓存展示

**学习内容：**

- Android Room DAO 同步查询与 LiveData 观察
- Kotlin Flow 的 channelFlow 实现"双发射"（先缓存后网络）
- Repository 模式在 Clean Architecture 中的定位——上连 ViewModel，下接 LocalDataSource 和 RemoteDataSource
- 离线优先（Offline-First）架构设计思想与在线优先的区别

**交付物：**

- UserRepository 重构代码（新增 getUserInfoWithCache() 方法，返回 Flow）
- LoanOrderRepository 重构代码
- AuthRepository 重构代码
- Room UserDao / LoanOrderDao 补全缺失的缓存查询方法（如 getUserInfoFromCache、getLoanOrdersFromCache）

---

#### A2. Token 加密存储

**学习目标：** 理解 AndroidKeyStore 系统级密钥管理机制（App 无法直接读取密钥本身），掌握 Jetpack EncryptedSharedPreferences 的使用，明确 Android 应用安全存储的最佳实践与降级策略

**任务：** 用 EncryptedSharedPreferences 替换 TokenManager 中的普通 SharedPreferences，对外 API 保持不变

**学习内容：**

- AndroidKeyStore：系统服务，密钥存储在硬件安全模块（TEE）中
- MasterKey.Builder：用 AES256_GCM 生成主密钥
- EncryptedSharedPreferences：AES256_SIV 加密 key + AES256_GCM 加密 value
- Android 6.0+ 支持情况 + 不支持时的降级策略

**交付物：**

- TokenManager.java 重构代码（内部透明切换加密存储）
- build.gradle 添加依赖 `androidx.security:security-crypto:1.1.0-alpha06`
- 安全性验证说明（Root 设备上 token 文件内容对比）

---

#### A3. FragmentRouter 统一路由层

**学习目标：** 掌握 FragmentTransaction.setCustomAnimations 的四参数含义（enter/exit/popEnter/popExit），理解 addToBackStack 的正确使用时机与滥用风险，掌握 Material Motion 标准动画曲线

**任务：** 新建 FragmentRouter 工具类，所有 Fragment 跳转统一走入口，全局控制 Material Motion 动画与返回栈策略

**学习内容：**

- FragmentTransaction.setCustomAnimations 的四个动画资源参数
- fast_out_slow_in 插值器（Material Design 标准曲线，先快后慢模拟物理运动）
- addToBackStack：详情页进入加，Tab 间切换不加
- popBackStack（异步）vs popBackStackImmediate（同步，容易 IllegalStateException）
- FragmentManager.isStateSaved() 防崩溃检查

**交付物：**

- FragmentRouter.java（push 方法统一动画 + 返回栈策略、pop 方法安全返回）
- res/anim/slide_in_right_fast_out_slow_in.xml
- res/anim/slide_out_left_fast_out_slow_in.xml
- res/anim/slide_in_left_fast_out_slow_in.xml
- res/anim/slide_out_right_fast_out_slow_in.xml
- 全项目 Fragment 跳转点替换清单（LoanFragment、ApplicationRecordsFragment 等）

---

#### A4. RecyclerView 流畅度三板斧

**学习目标：** 深入理解 RecyclerView 的复用机制（ViewHolder Pool、Scrap、Dirty Flag），掌握 Glide override 降内存原理，理解 DiffUtil 局部刷新的计算流程

**任务：** 对项目中所有 RecyclerView Adapter 应用三板斧——Glide override 实际尺寸 + setHasStableIds + DiffUtil 局部更新

**学习内容：**

- RecyclerView.setHasStableIds(true) + getItemId(int position) 返回业务主键
- Glide.override(w, h)：不加载原图，只加载 ImageView 实际显示尺寸
- DiffUtil.Callback 四方法：areItemsTheSame（是否同一个对象）、areContentsTheSame（内容是否变了）
- DiffUtil.calculateDiff 的 bestSize 参数与计算耗时平衡
- RecyclerView.onViewRecycled 中 Glide.clear() 必须调用

**交付物：**

- ImageLoader.java 修改（新增 override(targetW, targetH) 参数）
- 所有 Adapter 添加 setHasStableIds(true) + getItemId 实现
- 每个 Adapter 新增 DiffCallback 内部类 + updateData(List) 方法替换 notifyDataSetChanged
- RecyclerView.onViewRecycled 统一加 Glide.clear()
- CrossFade 动画从 300ms 缩短到 150ms

---

### UI 层

---

#### U1. 骨架屏 + 统一状态管理

**学习目标：** 理解 Loading/Success/Error/Empty 四种页面状态的切换逻辑，掌握 Facebook Shimmer 骨架屏动画实现原理，建立 BaseFragment 基类模式

**任务：** 所有列表页改造——骨架屏替代 ProgressBar，新增 Error/Empty 状态各含交互按钮，BaseDetailFragment 加 applyState() 统一切换

**学习内容：**

- Facebook Shimmer 库：SkeletonLayout + ShimmerFrameLayout 闪光效果
- FrameLayout 多层 View 叠加实现状态切换
- View.setVisibility(VISIBLE/GONE) 的主线程约束
- BaseFragment 基类：子类只实现 getLayoutId() + 四个 View id，基类统一管理状态

**交付物：**

- build.gradle 添加 shimmer 依赖
- res/layout/skeleton_loan_list.xml（骨架屏模板）
- res/layout/state_error.xml（错误状态 + 重试按钮 + 插画）
- res/layout/state_empty.xml（空状态 + "去申请"引导按钮 + 插画）
- BaseDetailFragment.java 重构（新增 PageState 枚举 + applyState 方法）
- 所有列表页改造清单

---

#### U2. 深色模式完整适配

**学习目标：** 掌握 values-night 资源目录限定符的优先级规则，理解 AppCompatDelegate 的三种深色模式切换，学习 Material3 DynamicColor 自动取色

**任务：** 所有颜色、背景、状态栏、图标添加深色版本，设置页面加深色模式切换开关

**学习内容：**

- values-night/ 限定符：系统深色模式时自动加载该目录资源
- AppCompatDelegate.setDefaultNightMode：MODE_NIGHT_FOLLOW_SYSTEM / MODE_NIGHT_NO / MODE_NIGHT_YES
- Material3 DynamicColor（Android 12+）：从用户壁纸自动提取主题色
- WindowInsetsController：深色状态栏文字（isAppearanceLightStatusBars = false）
- WCAG AA 对比度要求（前景色与背景色对比度 ≥ 4.5:1）

**交付物：**

- res/values-night/colors.xml（所有颜色的深色版本）
- res/values-night/themes.xml（深色主题）
- SettingsFragment 加深色模式三选项开关（跟随系统 / 浅色 / 深色）
- AndroidManifest.xml Application 加 forceDarkAllowed 属性
- 状态栏/导航栏颜色随模式切换的工具类

---

#### U3. 界面美化

**学习目标：** 掌握 Canvas.drawArc 绘制圆环仪表盘、ValueAnimator + FloatEvaluator 数字滚动、Lottie JSON 动画接入、Material Shape 不规则卡片

**任务：** 关键页面添加动效和视觉细节——还款成功页 Lottie、信用分圆环仪表盘、金额数字滚动、卡片阴影 ripple、下拉刷新 Lottie

**学习内容：**

- Canvas.drawArc 圆环 + paint.setTextAlign 中心文字绘制
- ValueAnimator.ofFloat(from, to) + addUpdateListener 回调中刷新 TextView
- Lottie：加载 JSON 动画文件、setAnimation、playAnimation
- Material Shape：shapeAppearanceOverlay 实现不规则圆角（三角形/梯形）
- 下拉刷新 OnRefreshListener + 自定义 RefreshLayout

**交付物：**

- 3-5 个 Lottie JSON 动画文件（还款成功勾选、下拉刷新、空状态）
- CreditScoreDialView.java（Canvas 绘制圆环仪表盘，支持 0→目标值动画）
- NumberRollingTextView.java（ValueAnimator 实现金额数字滚动）
- 产品卡片 Material Shape + elevation 阴影 + ripple drawable
- 空状态 SVG 插画（Android VectorDrawable）
- 下拉刷新 Lottie 头部配置

---

### 功能层

---

#### P1. 人脸二次验证

**学习目标：** 掌握 Jetpack BiometricPrompt API，理解 BiometricManager 设备能力检测，学习 CryptoObject + AndroidKeyStore 的安全绑定模式

**任务：** 大额还款贷款等敏感操作前，弹出系统人脸验证弹窗，验证通过才执行后续操作

**学习内容：**

- BiometricPrompt.Builder：setAllowedAuthenticators 只允许 FACE（BIOMETRIC_STRONG | FACE）
- BiometricPrompt.PromptInfo：标题、副标题、描述、否定按钮文案、不允许取消
- BiometricManager.canAuthenticate()：检测设备是否支持人脸 + 是否有录入的人脸
- CryptoObject：绑定到 BiometricPrompt，确保验证结果不可被篡改
- 降级策略：不支持人脸时退到密码验证

**交付物：**

- BiometricAuthManager.java（封装设备检测、弹窗展示、结果回调）
- PaymentFragment.java 接入（executePayment() 前调 BiometricAuthManager.authenticate()）
- PasswordLoginFragment / UsernameEditFragment / MyBankCardsFragment 接入
- SettingsFragment 加"安全中心 → 人脸验证开关"
- AndroidManifest.xml 加 USE_BIOMETRIC 权限

---

#### P2. 防截屏 + 安全键盘

**学习目标：** 理解 FLAG_SECURE 在 SurfaceFlinger 层防截屏/录屏的原理，掌握自定义 EditText 安全输入的实现

**任务：** 敏感页面（支付、绑卡、改密码）加 FLAG_SECURE，密码输入用 SecureEditText

**学习内容：**

- WindowManager.LayoutParams.FLAG_SECURE：SurfaceFlinger 层阻止截图/录屏/投射
- FLAG_SECURE 生命周期：onResume 添加，onPause 移除（避免影响其他页面）
- EditText 安全输入：disableAutofill / disableClipboard / disableTextSuggestions
- TYPE_TEXT_VARIATION_PASSWORD / TYPE_CLASS_NUMBER 安全键盘类型

**交付物：**

- SecureEditText.java（禁用自动填充、剪贴板、输入法建议，强制安全键盘）
- BaseDetailFragment.java 修改（onResume 加 FLAG_SECURE，onPause 移除）
- 所有密码输入框替换为 SecureEditText
- AndroidManifest.xml 加 USE_BIOMETRIC 权限

---

#### P3. 滑块行为验证

**学习目标：** 掌握 Android 自定义 View 的 onMeasure/onLayout/onDraw/onTouchEvent 完整流程，理解行为式验证反机器人原理

**任务：** 自定义滑块验证 View 接入登录流程，账号密码正确后先验证滑块才发登录请求

**学习内容：**

- View.onTouchEvent(MotionEvent)：ACTION_DOWN/ACTION_MOVE/ACTION_UP 状态处理
- Canvas.drawRoundRect 绘制轨道、drawCircle 绘制滑块
- ValueAnimator：验证成功时滑块弹回带弹性、失败时震动
- MotionEvent 轨迹采集：采集坐标序列，计算速度/加速度/抖动特征
- 行为式验证：真人滑动有加速度变化和抖动，机器人匀速直线

**交付物：**

- SliderVerifyView.java（自定义滑块 View + 轨迹采集 + 验证回调接口）
- SliderVerifyDialogFragment.java（弹窗形式包装）
- LoginActivity.java 接入（账号密码 → 弹滑块验证 → 通过才发登录请求）

---

#### P4. 智能客服快捷卡片 + 对话历史

**学习目标：** 掌握 RecyclerView.ItemViewType 多类型 ViewHolder 实现、Room 消息存储与查询、DiffUtil 插入动画

**任务：** 客服页加快捷问题卡片（首次进入展示 4-6 个预设问题，点击自动发送），每次进入客服页调用后端接口拉取历史对话；Room 做本地缓存加速加载

**学习内容：**

- RecyclerView.ItemViewType：getItemViewType 返回类型，onCreateViewHolder 根据类型创建不同 ViewHolder
- DiffUtil.dispatchUpdatesTo：插入新消息时带动画
- Retrofit 定义对话历史接口（ GET /api/chat/history?sessionId=xxx ）
- Room 做本地缓存：网络请求 → 成功后写 Room → UI 读 Room 展示（配合 A1 的先缓存后网络策略）
- SSE 新消息到达 → 即时插入列表 → 同时写 Room + 写后端

**交付物：**

- Retrofit ChatApi 接口新增 getChatHistory(sessionId) 方法
- ChatMessage Entity + Dao（Room 本地缓存表，支持 queryBySession 快速查 + insert 写入）
- ChatRepository
- SmartCustomerServiceActivity 重构（多类型 Adapter：用户气泡右蓝 / 系统气泡左白 / 快捷卡片底部横排）
- 首次进入客服页展示 4 个预设问题卡片

---

#### P5. 收支统计图

**学习目标：** 掌握 MPAndroidChart 四种图表配置 + 自定义 Formatter 数据格式化

**任务：** 新建财务报表页面，展示 4 种统计图表——贷款类型饼图、近 6 月还款折线图、本金利息环形图、每月收支柱状图

**学习内容：**

- PieChart：setCenterText + setDrawEntryLabels(false) + setValueTextSize
- LineChart：setMode(LineDataSet.Mode.CUBIC_BEZIER) 平滑曲线 + setDrawCircle 数据点
- BarChart：BarData 分组柱状图 + BarEntry(x, float[])
- IAxisValueFormatter：自定义 X 轴 "1月/2月..."
- ValueFormatter：自定义 "¥4,523" 千分位格式

**交付物：**

- ReportsFragment.java（4 图表配置代码 + 数据从 Room 或后端接口获取）
- build.gradle 添加 MPAndroidChart 依赖
- ProfilereFragment 加"收支统计"入口
- 报表数据接口对接

---

#### P6. App Shortcuts + 桌面小组件

**学习目标：** 掌握 ShortcutManager 静态配置 + Deep Link 跳转，学习 Glance（Jetpack Compose for Widgets）实现桌面小组件

**任务：** 长按 App 图标显示三个快捷操作（快速还款 / 查看额度 / 客服），桌面添加小组件显示下一期还款日和金额

**学习内容：**

- shortcuts.xml：静态声明 shortcut，用 intent-filter 实现 Deep Link
- ShortcutInfo.Builder：动态添加/更新/禁用 shortcut
- GlanceAppWidget：@Composable 写法实现小组件 UI（Text / Image / Button 都是 composable）
- GlanceAppWidgetReceiver：AndroidManifest 中声明
- PeriodicWorkRequest：每 6 小时定时刷新小组件
- Action：小组件点击事件，用 setLaunchIntentProvider 打开指定页面

**交付物：**

- res/xml/shortcuts.xml（三个静态 shortcut）
- AndroidManifest.xml Application 加 android:resource="@xml/shortcuts"
- RepaymentWidget.kt（GlanceAppWidget 实现，显示下一期还款日 + 金额，点击打开 PaymentFragment）
- WidgetConfig.xml（小组件配置：minWidth / minHeight / resizeMode）
- WorkManager 定时刷新小组件

---

#### P7. 还款提醒 WorkManager

**学习目标：** 理解 WorkManager 的 OneTimeWorkRequest 定时调度 + NotificationChannel 完整配置

**任务：** 还款到期前 3 天、1 天、当天早上 9 点各发一条系统通知，还款成功后自动调度下一期提醒

**学习内容：**

- OneTimeWorkRequest + Calendar：根据下次还款日计算延迟时间，setInitialDelay(duration, TimeUnit)
- NotificationCompat.Builder：setContentTitle / setStyle(BigTextStyle) / setSmallIcon
- NotificationChannel：8.0+ 必需，设置重要性 HIGH、声音、锁屏显示
- PendingIntent：用 getActivity 打开 PaymentFragment，FLAG_IMMUTABLE 防止 token 泄漏
- Android 13+ POST_NOTIFICATIONS 运行时权限
- WorkManager.enqueueUniqueWork：防止重复调度

**交付物：**

- RepaymentReminderWorker.kt（读下次还款日 → 算倒计时 → 满足条件发通知 → 否则等下次）
- NotificationChannel 初始化代码（MyApplication 中注册）
- 还款成功后的调度逻辑（PaymentViewModel 还款成功回调里 enqueue UniqueWork）
- SettingsFragment 加"提醒设置"入口（用户自定义提前几天提醒）
- AndroidManifest.xml 加 POST_NOTIFICATIONS 权限声明
