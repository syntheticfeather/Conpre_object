# 每周任务总结

## 第一周（2026-03-09至2026-03-15）

### 一、安全性增强

1. **完善表单验证规则**
   - 为登录注册表单添加实时验证反馈

### 二、代码优化

1. **优化API请求**
   - 统一请求/响应处理
   - 在request.js响应拦截器中统一处理错误提示
2. **重构组件结构，提高复用性**
   - 优化`BaseTable`组件，支持更多Ant Design Table原生属性透传
   - 复用公共组件`BaseTable`减少代码重复
   - 统一表格操作列的按钮基础样式和交互逻辑
3. **优化状态管理**
   - 使用Pinia统一管理全局状态
   - 添加Pinia持久化插件，实现关键状态的本地存储
   - 优化Store模块结构，统一action命名规范
   - 添加Store状态变更日志，便于调试
4. **统一代码风格**
   - 配置ESLint和Prettier规则；
   - 统一Vue组件编写规范（script setup优先）
   - 统一CSS命名规范（BEM或scoped样式）

### 三、文档完善

1. **编写前端架构文档**（`frontend/document/架构说明.md`）
   - 项目目录结构说明
   - 各模块职责划分
   - 组件层级关系图
   - 状态管理流程图
2. **编写组件使用文档**（`frontend/document/组件说明.md`）
   - BaseTable组件属性和事件说明
   - BasePagination组件使用示例
   - 自定义插槽使用指南

### 四、优化显示

1. 修复侧栏和增加产品页面部分内容出现在顶栏上层的问题。
2. 修改部分样式。

***

## 第二周（2026-03-16至2026-03-22）

### 一、完善功能

1. 产品批量处理
   - 完善产品的批量上架、下架、删除功能
2. 产品选项管理
   - 完善产品选项单独增删、批量增加的功能
3. 用户详情内容完善
   - 增加用户详细信息展示

### 二、新增

1. 认证材料图片查看
   - 支持查看工作证明、第三方认证、不动产证明材料
2. 管理员上传头像
   - 实现管理员头像上传功能
   - 支持头像预览和更新
3. 根据信誉分搜索用户
   - 支持按信誉分排序（升序/降序）
4. 页面美化-顶栏内容
   - 增加信息查看
   - 隐藏退出按钮
   - 增加待定义搜索框

### 三、数据可视化-增加风险管理页面图表

1. 数据统计
   - 实现用户贷款情况统计图表
   - 实现贷款申请统计图表
   - 展示贷款申请趋势
2. 风控数据
   - 提供用户信用分分布数据
   - 提供申请通过率数据
   - 提供风险等级统计

***

## 第三周（2026-03-23至2026-03-29）

### 一、安全性增强

1. **Token 刷新机制**
   - 创建前端 JWT 工具类 ( src/utils/jwt.js )：实现 token 解码、过期检查、即将过期检查和用户信息获取功能
   - 修改应用启动逻辑 ( src/main.js )：添加认证状态检查，实现自动 token 刷新和登录状态恢复
   - 优化路由守卫 ( src/router/index.js )：添加 token 过期检查，确保路由跳转时的认证状态处理
   - 实现无感登录恢复、提前刷新机制、统一认证状态管理和错误处理优化

### 二、数据可视化优化

1. **风险管理页面图表优化**
   - 调整图表布局：将贷款申请和用户注册趋势拆分为两个独立图表，并排显示
   - 优化 tooltip 显示：实现四象限智能定位，避免遮挡图表中间文字
   - 数据展示优化：
     - 贷款申请状态分布和用户贷款状态分布图表改为显示百分比
     - 风险等级统计表添加无风险柱状图
     - 用户信用分统计和风险等级统计图表添加柱状体数值标注
   - 日期间距调整：优化 x 轴标签显示，旋转 30 度并调整字体大小

### 三、页面美化

1. **布局优化**
   - 侧栏处理：确保侧栏始终在左侧居中显示
   - 整体配色：优化页面配色方案，提升视觉体验
2. **页面标题优化**
   - 统一页面标题基础字体大小和颜色
   - 优化表单布局和输入框样式
   - 确保各页面元素的对齐和间距一致性
3. **产品页面样式优化**
   - 调整产品信息和选项区域布局，添加分割线
   - 优化期限输入显示，解决数值被遮挡问题
   - 添加利率百分比后缀，实现百分比符号显示在输入框内
   - 实现产品选项区域的垂直滚动，解决内容溢出问题
4. **用户信息页面优化**
   - 修改 ApplicationDetailModal.vue 中用户基本信息部分，采用两列 flex 布局

5.3. **导航菜单优化**

- 调整侧栏菜单和顶栏菜单的布局和样式
- 优化菜单交互逻辑，提升用户体验

***

## 第四周（2026-03-30 至 2026-04-05）

### 一、新增显示内容

1. **风险管理页面数据展示**
   - 四个统计卡片：用户总数、本月申请数、贷款总额、逾期率
   - 月度审批统计图表：AI 审批 vs 人工审批对比
   - 用户贷款状态分布图（饼图）
   - 贷款申请状态分布图（饼图）
   - 风险等级统计表（柱状图）
   - 贷款用途分布列表
   - 最新动态列表-仅展示
2. **导航栏优化**
   - 面包屑导航显示当前页面路径
   - 主题切换图标（太阳/月亮）
   - 管理员信息区域（头像 + 用户名）
   - 下拉菜单：修改头像、退出登录按钮
3. **表格表头样式统一**
   - 所有表格表头使用统一的灰蓝色背景（`#8b92a9`）
   - 表头文字使用白色，提升可读性
   - 覆盖范围：用户管理表、产品管理表、申请表、订单表等

### 二、主题切换功能

1. **深色模式支持**
   - 建立完整的 CSS 变量体系
   - 一键切换亮色/深色模式
   - 所有页面元素自动适配主题
   - 0.3 秒平滑过渡动画
2. **适配的组件**
   - 导航栏、侧边栏、表格
   - 风险管理页面所有卡片和图表
   - 面包屑导航、主题图标

### 三、代码优化

1. **CSS 变量管理**
   - 统一在 `:root` 和 `html.dark` 中定义主题变量
   - 使用语义化变量名（如 `--table-head-bg`）
2. **样式复用**
   - 移除硬编码颜色值
   - 使用 CSS 变量

***

## 第五周（2026-04-06 至 2026-04-13）

### 一、文件变动

#### 1. **数据大屏视图**

- **`src/views/risk/DVScreenView.vue`** ⭐ 核心新增
  - 完整的 3D 地图 + 数据大屏可视化页面
  - 集成了 ECharts 图表库
  - 包含 8 个图表组件：
    - 左侧：每月注册人数柱状图、用户风险等级环图、在线人数波浪图、每月贷款申请数量柱状图
    - 右侧：贷款申请用途胶囊柱图、用户状态环图、每月放款/还款金额双线波浪图、数据集柱状图
  - 使用 Three.js 实现 3D 地图
  - 集成了 DataV 装饰组件

#### 2. **工具函数库**

- **`src/utils/Map3d.js`**
  - 3D 地图核心工具类
  - 基于 Three.js 封装
  - 处理地图渲染、相机控制、光照等
- **`src/utils/index.js`**
  - 通用工具函数

#### 3. **自定义 Hooks**

- **`src/hooks/`** 目录
  - `useFileLoader.js` - 文件加载钩子
  - `useCoord.js` - 坐标处理钩子
  - `useConversionStandardData.js` - 数据标准化转换钩子
  - `useCountryMesh.js` - 国家网格生成钩子

#### 4. **静态资源**

- **`public/bg1.jpg`** / **`public/bg2.jpg`**
  - 数据大屏背景图片

#### 5. **删除文件**

- **`public/860.jpg!detail.jpg`**
  - 删除了旧的图片资源

### 二、页面具体内容

#### 1. **数据大屏可视化系统**

- 3D 中国地图展示（基于 Three.js）
- 8 个 ECharts 图表组件
- 响应式布局设计

#### 2. **路由体系完善**

- 新增数据大屏独立路由 `/dv-screen`
- 完善侧边栏导航结构

***

## 第六周（2026-04-14 至 2026-04-21）

### 一、交互体验优化

1. **侧栏收起延时优化**
   - 优化侧边栏收起动画，减少延迟时间
   - 改善用户操作流畅度，提升界面响应速度

### 二、数据大屏功能增强

1. **地图显示优化**
   - 实现地图侧边锯齿平滑处理，提升视觉质量
   - 优化省份颜色配色方案，增强数据可读性
   - 添加选中省份移动到中心效果，伴随标题省份动态切换
2. **图表组件升级**
   - 增加年度注册人数柱状图，展示用户增长趋势
   - 实现3D环图效果，提升数据可视化层次感
   - 丰富图表样式和配色方案，统一视觉风格
   - 为所有图表添加悬浮小窗（tooltip）功能，显示详细数据
   - 优化资金流入流出图表为三线波浪图，并添加差值分析
   - 调整注册登录图表为重叠柱状图，直观对比注册与登录人数
   - 修改用户生命周期活跃度图表为按月份堆叠图，清晰展示不同用户群体活跃情况

### 三、实时消息系统

1. **导航栏stream实时消息实现**
   - 基于Fetch API实现SSE（Server-Sent Events）实时消息推送
   - 支持消息自动重连机制，确保连接稳定性
   - 添加桌面通知功能，及时提醒重要消息
   - 修复SSE连接401认证错误，优化token获取逻辑
   - 实时显示待审核申请数量，提升工作效率

### 四、用户详情页优化

1. **申请记录分页显示**
   - 实现用户详情页申请记录分页功能

### 五、代码质量与维护

1. **CSS样式重组与注释**
   - 按照页面布局顺序重排riskManageView\.css样式文件
   - 为每个主要区域添加清晰的注释说明，提升代码可维护性
   - 统一图表tooltip样式：半透明黑色背景（rgba(0,0,0,0.7)）、白色半透明边框（rgba(255,255,255,0.2)）、白色文字（#fff）

## 第七周（2026-04-22 至 2026-04-29）

### 一、页面美化

#### 1. 数据大屏重构优化
- **图表组件拆分**（`src/components/risk/charts/`）
- **图表工具抽象**：新增 `useCharts.js` hook 统一 ECharts 实例管理，新增 `pieTooltipHelper.js` 统一饼图悬浮提示
- **地图渲染优化**：优化 `Map3d.js`，重构 `useCountryMesh.js`，新增 `useMap3DCamera.js` 和 `useMapInteraction.js` 分离相机控制和交互逻辑

#### 2. 导航栏与侧栏美化
- **NavbarMenu.vue 重构**：重新设计搜索框布局，优化主题切换图标（Sunny/Moon），集成 SSE 实时通知中心（消息面板、全部已读、清空消息等操作）
- **SidebarMenu.vue 优化**：调整菜单项间距和折叠动画，提升交互流畅度
- **DashboardView.vue 布局调整**：优化侧栏和导航栏的 flex 布局与 sticky 定位，确保内容区域适配

#### 3.其他页面样式优化
- **UserDetailPanel**：新增独立样式文件（`userDetailPanel.css`），优化用户详情信息展示
- **ImagePreview.vue**：优化图片预览遮罩层样式和工具栏交互
- **UserManageView.vue**：用户管理表格和筛选区域样式微调

### 二、功能完善

#### 1. 延期审核功能
- **API 层扩展**（`api/modules/application.js`）：新增延期申请相关接口
- **状态管理**（`stores/application.js`）：新增 `pendingPostponeRequests` / `completedPostponeRequests` 状态管理，支持延期申请列表获取和审核操作
- **待审核页面增加**（`PendingApplicationView.vue`）：新增延期申请审核模式，支持动态切换贷款申请/延期申请两种审核列表
- **审核详情弹窗重构**（`ApplicationDetailModal.vue`）：大幅重构（+369 lines），新增延期审核操作面板，支持审核通过/拒绝操作，展示延期详情信息
- **路由更新**：新增延期审核相关路由配置

#### 2. 消息系统完善
- **API 层扩展**（`api/modules/notification.js`）：新增通知消息的增删改查接口
- **导航栏实时消息中心**（`NavbarMenu.vue`）：SSE 消息推送集成，消息面板支持批量已读、单条删除、全部清空等操作

#### 3. 产品录入分页优化
- **AddProductView**：实现产品表单前四条/后四条分页显示设计
- 优化表单填写流程，支持分页状态保持，避免切换页面时数据丢失
 
#### 4. 智能客服新增相关页面与功能
- **MCPToolsView.vue**：已注册工具列表展示与关键词搜索，启用/禁用开关，工具调用日志筛选查看
- **KnowledgeManagementView.vue**：知识条目 CRUD（问题/答案/分类表单），关键词+分类搜索筛选，独立编辑弹窗，分页展示
- 以上页面均集成 ChatDialog 智能对话调试入口（右下角 FAB 悬浮按钮）

## 第八周（2026-05-02 至 2026-05-09）

### 单页面优化

#### 1. 风险管理页面
- RiskManageView.vue：移除顶部欢迎区（welcome-section），删除数据大屏/待审核跳转按钮；`pendingCount` 改为 `stats.pendingApplications` 直接管理；清理未使用 imports

#### 2. 用户管理页面
- 用户详情页：重新排版修改了整个页面

#### 3. 申请审批页面
- PendingApplicationView.vue：顶部新增欢迎区（渐变背景、日期信息、数据大屏按钮），切换按钮组 
- CompletedApplicationView.vue：样式微调
- ApplicationDetailModal.vue：使用 `el-collapse-transition` 包裹详情面板，实现折叠动画效果

#### 4. 系统管理页面
- AgentConfigView.vue：**从工具配置管理重写为 Prompt 提示词管理系统**，集成 promptsAPI 实现 CRUD、激活/停用、版本管理
- KnowledgeManagementView.vue：新增文件上传功能（支持图片预览/文件图标显示）静态展示
- MCPToolsView.vue：输出列使用 ContentTooltip 展示长文本，`padding-top: 0`；搜索工具 API 调用重构、数据兼容数组/对象格式

#### 5. 仪表盘导航与布局
- SidebarMenu.vue：菜单项选中样式优化
- DashboardView.vue：布局与样式微调

### 非单页面优化

#### 1. CSS 变量化改造
将部分硬编码颜色值替换为 CSS 变量，以便于支持亮色/深色主题切换：
- base.css：新增大量 CSS 变量，涵盖所有组件和页面的颜色配置

#### 2. border-radius 统一
将大部分组件元素的 `border-radius` 统一为 12px

#### 3. 聊天 SSE 重构
- useChatSSE.js：完全重构 SSE 重连机制，分离消息解析与事件处理；新增重试计数器和主动中止标记
- ChatDialog.vue：使用 `v-model` 替代 `@close` 事件控制对话框显隐

#### 4. 新增文件
- ContentTooltip.vue：文本内容提示框组件，支持长文本展示和样式定制
- LoadingDots.vue：自定义下拉列表的加载动画组件（四点旋转动画）
- prompts.js：Prompts API 模块，提供提示词的增删改查/激活/停用接口

#### 5. 项目配置与 API 扩展
- vite.config.js：新增 `/api/upload` 和 `/api/prompts` 代理配置
- api/index.js：新增 `promptsAPI` 导出

## 第九周（2026-05-16 至 2026-05-26）

### 单页面优化

#### 1. 产品管理页面架构调整
- **ProductManageView.vue**：
  - 将产品详情面板（ProductDetailPanel）从 ProductTable 组件内嵌提升至页面级管理
  - 实现产品列表与详情面板的切换显示模式（`v-show` / `v-if` 控制）
  - 通过事件机制（`product-selected`）实现表格到详情的导航
  - 新增 `handleProductSelected`、`closeProductDetail`、`handleProductSaved` 方法处理状态切换

#### 2. 增加产品页面
- **AddProductView.vue 布局重构**：步骤1改为左右分栏布局（左侧文本输入 2/3，右侧数值输入 1/3），步骤2仅保留产品选项编辑表格
- **表单验证与对齐修复**：统一使用 `hide-required-asterisk` 隐藏必填标记，所有表单项保留 `prop` 保持 DOM 结构一致，修复标签起始位置偏移问题
- **输入体验优化**：最短/最长期限输入框添加"个月"后缀，最小/最大金额输入框添加"元"后缀；期限步长下拉选项显示"X个月"，选中值居中显示
- **刷新机制**：产品添加成功后自动执行 `router.go(0)` 刷新页面
- **默认值调整**：最长期限默认值修改为 13；期限步长默认值改为 2；最小金额改为 1000，最大金额改为 500000

#### 3. 产品详情面板全面重构
- **ProductDetailPanel.vue UI 重新设计**：
  - 新增顶部导航栏：返回按钮 + 产品名称 + 状态徽章（在线/下线）
  - 新增 Hero 卡片区域：渐变背景（蓝紫渐变）+ 产品分类 + 平均利率大字显示
  - 新增 5 个统计卡片网格布局：金额范围、平均利率、最低利率、最高利率、上线天数
- **功能增强**：
  - 新增产品上下架切换功能（`toggleStatus`），带确认对话框提示
  - 编辑表单改为左右分栏布局（el-row 16:8 比例）
  - 期限步长智能计算：新增 `validSteps`、`canSelectStep` 计算属性，`onTermRangeChange` 方法实现范围变化时自动重置步长
  - 数据展示增强：新增 `formatAmount`（万/亿格式化）、`getAvgRate`、`getMinRate`、`getMaxRate`、`getOnlineDays`、`getTermRange` 等工具方法

#### 4. 申请审批详情弹窗过渡动画扩展
- **ApplicationDetailModal.vue**：
  - 为 `<el-collapse-transition>` 添加 `@after-enter="onTransitionComplete"` 事件监听
  - 新增 emit 事件 `'transition-complete'`，在折叠/展开动画完成后通知父组件
  - 支持父组件在动画结束后执行滚动定位等操作
- **CompletedApplicationView.vue与PendingApplicationView.vue**：
  - 监听 `transition-complete` 事件，实现详情面板展开后自动滚动到可视区域顶部
  - 新增 `scrollToDetail` 方法，使用 `scrollIntoView({ behavior: 'smooth' })` 平滑滚动

#### 5. 用户详情面板敏感信息交互优化
- **UserDetailPanel.vue**：
  - 身份证号支持点击切换完整显示与脱敏显示（`showIdCardFull` 状态控制）
  - 银行卡号支持点击切换完整显示与脱敏显示（`showBankCardFull` 状态控制）
  - 新增 `formatBankCard` 方法：银行卡号脱敏格式化为 `**** **** ****` 分组显示格式
  - CSS 样式增强（userDetailPanel.css）：新增 `.info-row.clickable` 样式类
    - 鼠标悬浮时背景高亮（`#f5f7fa`）+ 手型光标（`cursor: pointer`）
    - 0.2 秒过渡动画 + 4px 圆角效果

#### 6. 智能客服相关页面
- **AgentConfigView.vue 提示词管理系统功能重构**（+135/-93 行）：
  - 功能分离：将原来的单一编辑模式拆分为"新增提示词表单" + "编辑对话框"
  - 新增 `editDialogVisible` 状态控制编辑弹窗显示
  - API 调用方式改变：`create` 方法参数从 `(name, content, version)` 改为 `(data)` 对象形式
  - 激活/停用操作优化：移除独立的 `activate/deactivate` 接口调用，改用 `update` 接口传递 `{is_active}` 字段
  - 表格列增强：新增"分类"（category）列和"创建时间"（created_at）列
  - 编辑交互优化：点击编辑按钮打开独立对话框（`openEditDialog`），避免与新建表单冲突
  - 代码清理：移除 `loadActivePrompt` 函数（不再需要加载激活状态到编辑区）；删除后不再检查是否正在编辑该项

- **MCPToolsView.vue MCP 服务与工具管理页面重大重构**（+430/-120 行）：
  - 架构升级：从单页面改为 Tab 双页面模式
    - Tab 1: MCP 服务器管理（完整 CRUD 功能）
    - Tab 2: 已注册工具管理（列表展示 + 启用/禁用开关）
  - MCP 服务器管理功能（全新开发）：
    - 表单字段支持：server_id（唯一标识）、transport（传输方式：SSE/stdio）、url/command（服务地址或命令）、args（命令参数数组）、timeout（超时时间）、api_key（API 密钥）
    - 动态表单切换：根据 transport 类型（SSE/stdio）显示不同的配置字段组合
    - 完整业务流程：服务器搜索（前端过滤）、添加、编辑（先删后加策略）、删除（带确认对话框）
  - 工具管理模块优化：
    - 迁移至新的 `toolsAPI` 模块（`@/api/modules/tools`），替代直接 `request` 调用
    - 搜索功能改为前端过滤（原为后端接口调用）
    - 工具描述列使用 ContentTooltip 组件展示长文本
    - 移除工具调用日志模块（后端接口暂未实现）
  - 新增依赖：引入 `ElMessageBox` 用于删除确认操作

- **KnowledgeManagementView.vue 文档上传功能开发**（+968 行，重大功能扩展）：
  - **已记录部分** ✅：分块列表优化（资源路径列、序号列等）、问答对列表优化
  - **新增文档上传功能**（未记录部分）：
    - FAQ 附件上传区域：UI 占位符样式（待后端支持，当前禁用状态 `cursor: not-allowed`）
      - 虚拟拖拽上传区域（90px 高度）+ 上传图标 + 提示文字 + 功能未开放警告提示
    - 文档上传按钮区域：图片上传 / 文档上传 / PDF 上传三个入口按钮，灰色背景容器
    - 文档上传演示区：完整的拖拽上传区域（100px 高度）+ 点击上传支持
      - 悬停效果：边框变蓝色（`#409eff`）+ 背景变浅蓝（`#ecf5ff`）
    - 上传结果列表（实时状态显示）：
      - 表头：文件名 / 状态（成功✓ / 失败✗ / 上传中⟳）
      - 成功项：绿色图标 + 文件名 + "上传成功"状态
      - 失败项：红色图标 + 文件名 + "上传失败"状态
      - 上传项：蓝色旋转动画图标 + 文件名 + "上传中..."状态
    - 上传指南区域：
      - 左侧蓝色边框标识 + 标题"上传说明"
      - 格式要求列表：支持格式（PDF/Word/Excel/TXT/图片）、大小限制（单个文件 ≤ 10MB）、命名规范
      - 注意事项提示（橙色文字）：建议批量上传前先压缩打包
    - 文档内容查看器：
      - 最大高度 70vh 可滚动区域
      - 元信息区：文档标题 + 上传时间
      - 正文预览区：浅灰背景（`#f5f7fa`）+ 16px 内边距 + 8px 圆角
      - 最大高度 400px 限制 + 垂直滚动支持
  - **CSS 样式大幅扩展**（+540 行）：
    - FAQ 上传相关：`.faq-attachment-upload`、`.faq-upload-icon`、`.faq-upload-text`、`.faq-upload-tip`
    - 文档上传相关：`.upload-actions`、`.doc-upload-demo`、`.doc-upload-icon`、`.doc-upload-text`
    - 结果列表相关：`.upload-result-list`、`.result-header`、`.result-item`（含 success/error/uploading 状态样式）
    - 上传指南相关：`.upload-guide`、`.guide-title`、`.guide-list`、`.guide-note`
    - 内容查看器相关：`.document-content-viewer`、`.content-meta`、`.content-body`、`.content-text`

### 非单页面优化

#### 2. 路由守卫逻辑重构
- **router/index.js 路由守卫优化**：
  - 移除冗余辅助函数：删除 `checkAuthFromStorage()` 和 `getRefreshTokenFromStorage()`
  - 内联认证检查逻辑：将分散的检查代码合并到 `beforeEach` 守卫函数内部
  - 增强错误处理机制：
    - JSON 解析失败时清除无效存储数据（`localStorage.removeItem`）并跳转登录页
    - Token 刷新失败时区分"接口返回非 200"和"网络异常"两种情况，分别记录 warn/error 日志
    - 无 refreshToken 时直接跳转登录（不再尝试无效的刷新请求）

#### 3. HTTP 请求工具文件上传适配
- **request.js 请求拦截器增强**：
  - 新增 FormData 类型检测：`config.data instanceof FormData`
  - 自动删除 `Content-Type` 请求头，让浏览器自动设置正确的 `multipart/form-data` 格式和 `boundary` 参数
  - 解决文件上传时的请求格式问题（特别是 KnowledgeManagement 页面的文档上传功能）

#### 4. Prompts API 接口调整
- **prompts.js API 模块更新**：
  - `create` 方法签名重构：从 `(name, content, version)` 多参数形式改为 `(data)` 对象参数
    - 对象结构：`{ name: string, content: object, version?: string }`
    - 与 AgentConfigView.vue 的创建逻辑配合，传递更清晰的数据结构
  - 移除独立接口方法：删除 `activate(id)` 和 `deactivate(id)` 两个方法
    - 原因：后端接口调整，激活/停用操作合并到 update 接口
    - 替代方案：通过 `update(id, { is_active: true/false })` 实现相同功能
  - 保留方法：`getAll`、`getActive`、`getById`、`update`、`delete` 不变

#### 5. MCP Tools API 模块新增
- **tools.js 新增文件**（`src/api/modules/tools.js`）：
  - 完整的 MCP 服务器管理接口（4 个）：
    - `getMCPServers()`：获取所有已配置的 MCP 服务器列表 (GET `/tools/mcp/servers`)
    - `getMCPServer(serverId)`：获取单个服务器详细信息 (GET `/tools/mcp/server/{serverId}`)
    - `addMCPServer(data)`：添加新的 MCP 服务器配置 (POST `/tools/mcp/server`)
    - `removeMCPServer(serverId)`：删除指定服务器配置 (DELETE `/tools/mcp/server/{serverId}`)
  - 工具管理接口（3 个）：
    - `getAll()`：获取所有已注册工具列表 (GET `/tools/`)
    - `search(name)`：按名称搜索工具 (GET `/tools/search?name=xxx`)
    - `updateStatus(toolName, enabled)`：更新工具启用/禁用状态 (PUT `/tools/{toolName}?enabled=true/false`)
  - 辅助功能接口（1 个）：
    - `refreshMCPTools()`：触发 MCP 工具刷新（重新连接服务器获取最新工具列表）(POST `/tools/mcp/refresh`)
  - 使用场景：支撑 MCPToolsView.vue 的完整 CRUD 功能和数据展示
