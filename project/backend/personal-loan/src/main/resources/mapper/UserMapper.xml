<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.personal_loan.mapper.UserMapper">

    <!-- 主结果映射 -->
    <resultMap id="UserDetailResultMap" type="com.example.personal_loan.dto.UserDetailResponse">
        <!-- 用户基本信息 -->
        <id property="user.id" column="user_id"/>
        <result property="user.userName" column="user_name"/>
        <result property="user.avatar" column="avatar"/>
        <result property="user.phone" column="phone"/>
        <result property="user.password" column="password"/>
        <result property="user.role" column="role"/>
        <result property="user.createTime" column="user_create_time"/>
        <result property="user.updateTime" column="user_update_time"/>

        <!-- 用户认证（一对一） -->
        <association property="userCert" javaType="com.example.personal_loan.entity.UserCert">
            <id property="userId" column="cert_user_id"/>
            <result property="idCard" column="id_card"/>
            <result property="creditScore" column="credit_score"/>
            <result property="bankCardId" column="bank_card_id"/>
            <result property="workCertId" column="work_cert_id"/>
            <result property="triCertId" column="tri_cert_id"/>
            <result property="immovableCertId" column="immovable_cert_id"/>
        </association>

        <!-- 贷款申请列表（一对多） -->
        <collection property="loanApplication" ofType="com.example.personal_loan.entity.LoanApplication">
            <id property="id" column="app_id"/>
            <result property="userId" column="app_user_id"/>
            <result property="productId" column="app_product_id"/>
            <result property="status" column="app_status"/>
            <result property="loanAmount" column="app_loan_amount"/>
            <result property="interestRate" column="app_interest_rate"/>
            <result property="loanPeriod" column="app_loan_period"/>
            <result property="term" column="app_term"/>
            <result property="repaidType" column="app_repaid_type"/>
            <result property="rejectReason" column="app_reject_reason"/>
            <result property="applyTime" column="app_apply_time"/>
            <result property="reviewTime" column="app_review_time"/>
        </collection>

        <!-- 订单列表（一对多） -->
        <collection property="order" ofType="com.example.personal_loan.entity.Order">
            <id property="id" column="order_id"/>
            <result property="userId" column="order_user_id"/>
            <result property="productId" column="order_product_id"/>
            <result property="status" column="order_status"/>
            <result property="repaidAmount" column="repaid_amount"/>
            <result property="loanAmount" column="order_loan_amount"/>
            <result property="interestRate" column="order_interest_rate"/>
            <result property="repaidType" column="order_repaid_type"/>
            <result property="loanPeriod" column="order_loan_period"/>
            <result property="term" column="order_term"/>
            <result property="currentTerm" column="current_term"/>
            <result property="contract" column="contract"/>
            <result property="overdueDays" column="overdue_days"/>
            <result property="startTime" column="start_time"/>
        </collection>
    </resultMap>

    <!-- 查询用户详情（包含所有贷款申请和所有订单） -->
    <select id="selectUserDetail" resultMap="UserDetailResultMap">
        SELECT
            u.id AS user_id,
            u.user_name,
            u.avatar,
            u.phone,
            u.password,
            u.role,
            u.create_time AS user_create_time,
            u.update_time AS user_update_time,

            uc.user_id AS cert_user_id,
            uc.id_card,
            uc.credit_score,
            uc.bank_card_id,
            uc.work_cert_id,
            uc.tri_cert_id,
            uc.immovable_cert_id,

            la.id AS app_id,
            la.user_id AS app_user_id,
            la.product_id AS app_product_id,
            la.status AS app_status,
            la.loan_amount AS app_loan_amount,
            la.interest_rate AS app_interest_rate,
            la.loan_period AS app_loan_period,
            la.term AS app_term,
            la.repaid_type AS app_repaid_type,
            la.reject_reason AS app_reject_reason,
            la.apply_time AS app_apply_time,
            la.review_time AS app_review_time,

            o.id AS order_id,
            o.user_id AS order_user_id,
            o.product_id AS order_product_id,
            o.status AS order_status,
            o.repaid_amount,
            o.loan_amount AS order_loan_amount,
            o.interest_rate AS order_interest_rate,
            o.repaid_type AS order_repaid_type,
            o.loan_period AS order_loan_period,
            o.term AS order_term,
            o.current_term,
            o.contract,
            o.overdue_days,
            o.start_time
        FROM users u
        LEFT JOIN user_certification uc ON u.id = uc.user_id
        LEFT JOIN loan_applications la ON u.id = la.user_id
        LEFT JOIN orders o ON u.id = o.user_id
        WHERE u.id = #{userId}
        ORDER BY la.apply_time DESC, o.start_time DESC
    </select>

    <!-- 按信用分搜索用户 -->
    <select id="selectUsersByCreditScore" resultType="com.example.personal_loan.dto.UserSearchDto">
        SELECT
            u.id,
            u.user_name AS userName,
            u.phone,
            u.avatar,
            u.role,
            uc.credit_score AS creditScore
        FROM users u
        LEFT JOIN user_certification uc ON u.id = uc.user_id
        <where>
            <if test="operator == 'gt'">
                uc.credit_score &gt; #{value}
            </if>
            <if test="operator == 'lt'">
                uc.credit_score &lt; #{value}
            </if>
            <if test="operator == 'eq'">
                uc.credit_score = #{value}
            </if>
            <if test="operator == 'ge'">
                uc.credit_score &gt;= #{value}
            </if>
            <if test="operator == 'le'">
                uc.credit_score &lt;= #{value}
            </if>
        </where>
        ORDER BY uc.credit_score DESC
    </select>
</mapper>