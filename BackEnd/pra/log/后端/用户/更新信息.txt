更新基本信息
    put请求
    @requestBody 直接将json 转换 成对象

    参数校验
    注解解决
    @requestBody @validated


    User中

    @notnull

    @email

    @notEmpty
    @pattern(regex = "^[a-zA-Z0-9]+$")

更新头像
    patch请求
    @requestParam @URL 参数校验
    同样三部曲
